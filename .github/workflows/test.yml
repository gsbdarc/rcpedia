name: DeployStanfordTest

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to run the workflow on'
        required: true
        default: 'QA'
  push:
    branches:
      - QA

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.MY_KNOWN_HOSTS }}" >> ~/.ssh/known_hosts
          echo "${{ secrets.MY_SSH_KEY }}" | base64 --decode > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Sync repository to server
        run: |
          rsync -e "ssh -i ~/.ssh/id_rsa -o ConnectTimeout=600 -o ServerAliveInterval=30 -o ServerAliveCountMax=5" \
            -av --delete . \
            rcpediaq@rcpedia-qa.su.domains:/home/rcpediaq/rcpedia-dev.stanford.edu/

      - name: Build MkDocs on server
        run: |
          ssh -i ~/.ssh/id_rsa -o ConnectTimeout=600 -o ServerAliveInterval=30 -o ServerAliveCountMax=5 \
            rcpediaq@rcpedia-qa.su.domains << 'EOF'
          cd /home/rcpediaq/rcpedia-dev.stanford.edu
          # If needed, install dependencies here (e.g., pip install -r requirements.txt)
          mkdocs build --clean
          EOF

  # check_links:
  #   needs: deploy
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.12.3'
  #     - name: Install linkchecker
  #       run: pip install linkchecker
  #     - name: Run linkchecker
  #       run: linkchecker --no-robots --check-extern -r 10 https://rcpedia-dev.stanford.edu
